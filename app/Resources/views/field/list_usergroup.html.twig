<div class="list-checkbox">
    <label>گروه های کاربری</label>
    <div class="row">
    {% for group in usergroup %}
        <div class="col-sm-6 col-md-4 col-lg-3"><label><input type="checkbox" name="group[]" value="{{ group.id }}"> {{ group.title }}</label></div>
    {% endfor %}
    </div>
</div>